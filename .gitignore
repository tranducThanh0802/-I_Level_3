# Bí mật — KHÔNG đưa lên repo chung
scripts/discord_config.json
scripts/.discord_state.json

# Rác hệ thống
.DS_Store
**/.DS_Store

# Feed live ephemeral (theo dõi lúc chạy, không phải hồ sơ)
agent-team/live/activity.jsonl
